{
  "name": "webbot-dominio-propio-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Worker de Cloudflare que reenvía los dominios propios de clientes (Cloudflare for SaaS) al host wildcard de Railway (WB-26).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "wrangler": "^4.0.0"
  }
}
